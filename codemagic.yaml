workflows:
  android-release:
    name: Android Release

    environment:
      java: 17

      android_signing:
        - upload_key

    scripts:
      - gradle assembleRelease

    artifacts:
      - app/build/outputs/**/*.apk
